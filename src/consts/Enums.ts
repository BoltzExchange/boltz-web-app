export enum SwapType {
    Submarine = "submarine",
    Reverse = "reverse",
    Chain = "chain",
}

export enum Denomination {
    Sat = "sat",
    Btc = "btc",
}

export enum Side {
    Send = "send",
    Receive = "receive",
}
